#include <stdio.h>
int main() {
    double a, b, rezultats;
    printf("Ieraksti divus ciparus: ");
    scanf("%lf %lf", &a, &b);  
 
    // Sareizina
    rezultats = a * b;

    // %.2lf Parada ciparus
    printf("Rezultats = %.2lf", rezultats);
    
    return 0;
}
